name: build_on_rpi4

on:
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: rpi4
    steps:
      - uses: actions/checkout@v2
      - name: Build install and reboot
        run: |
          # make
          # make install
          bash -c "(sleep 1; sudo reboot) &"
